{
  "name": "@lucid/8004-sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for LucidLayer — compute routing, validation, reputation, and payouts for ERC-8004 agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": ["erc-8004", "lucid", "ai-agents", "compute-routing", "x402"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
